<%= turbo_stream.replace "selected_players" do %>
  <%= render "selected_players", game: @game %>
<% end %>

<%= turbo_stream.replace "available_players" do %>
  <%= render "available_players", game: @game, players: Player.where.not(id: @game.players.ids) %>
<% end %>
