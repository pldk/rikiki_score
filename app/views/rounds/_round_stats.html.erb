<% total_predicted = round.predictions.sum { |p| p.predicted_tricks.to_i } %>
<% has_predictions = round.predictions.any? { |p| !p.predicted_tricks.nil? } %>

<%= has_predictions ? total_predicted - round.length : "-" %>
